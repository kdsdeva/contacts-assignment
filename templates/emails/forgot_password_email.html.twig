<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Registration confirm</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            font-size: 14px;
        }

        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
        }

        .header {
            background-color: #007bff;
            color: #fff;
            padding: 10px;
        }

        .header h1 {
            margin: 0;
        }

        .content {
            padding: 20px;
        }

        p {
            color: #262626;
        }

        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #2cafff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
        }

        .footer {
            padding: 20px;
            color: #262626;
        }
    </style>
</head>
<body>
<div class="email-container">
    <div class="header">
        <h2>Contact Admin</h2>
    </div>
    <div class="content">
        <h4>Hello {{ user.firstname }},</h4>
        <p>Click the below button to reset your password</p>
        <a href="{{ absolute_url(path('setnewpassword',{id: user.id, username: user.username})) }}" class="button">Click to reset</a>
    </div>
    <div class="footer">
        <div>Thanks & Regards</div>
        <div>Contact Admin</div>
    </div>
</div>
</body>
</html>